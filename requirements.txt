# Python requirements for tl_crossalloy.
# Install via:
#   conda create -n xtb_env python=3.11 -c conda-forge \
#       numpy scipy pandas scikit-learn rdkit matplotlib networkx paramiko
#   pip install -r requirements.txt
#
# (rdkit and scipy are best installed via conda-forge; the remaining
#  packages below go cleanly through pip.)

numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
networkx>=3.0
paramiko>=3.0
rdkit>=2023.9

# Only needed for ChemProp runs
chemprop>=2.2
lightning>=2.0
torch>=2.0
